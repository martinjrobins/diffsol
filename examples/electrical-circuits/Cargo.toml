[package]
name = "electrical-circuits"
version = "0.1.0"
edition.workspace = true
publish = false

[dependencies]
diffsol = { path = "../../crates/diffsol", features = ["diffsl-cranelift"] }
nalgebra = { workspace = true }
plotly = { workspace = true }